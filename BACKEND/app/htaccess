<IfModule mod_rewrite.c>
  RewriteEngine On

RewriteBase /tfg/

RewriteRule ^index\.html$ - [L]

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

RewriteRule . /tfg/index.html [L]
</IfModule>
